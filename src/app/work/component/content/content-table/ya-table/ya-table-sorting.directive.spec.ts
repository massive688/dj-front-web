import { YaTableSortingDirective } from './ya-table-sorting.directive';

describe('YaTableSortingDirective', () => {
  it('should create an instance', () => {
    const directive = new YaTableSortingDirective();
    expect(directive).toBeTruthy();
  });
});
