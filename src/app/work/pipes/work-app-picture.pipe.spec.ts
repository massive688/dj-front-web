import { WorkAppPicturePipe } from './work-app-picture.pipe';

describe('WorkAppPicturePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkAppPicturePipe();
    expect(pipe).toBeTruthy();
  });
});
