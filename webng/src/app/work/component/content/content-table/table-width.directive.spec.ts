import { TableWidthDirective } from './table-width.directive';

describe('TableWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new TableWidthDirective();
    expect(directive).toBeTruthy();
  });
});
