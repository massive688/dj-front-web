import { WorkCardBlurDirective } from './work-card-blur.directive';

describe('WorkCardBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new WorkCardBlurDirective();
    expect(directive).toBeTruthy();
  });
});
