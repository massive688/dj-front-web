import { WorkProfilePicturePipe } from './work-profile-picture.pipe';

describe('WorkProfilePicturePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkProfilePicturePipe();
    expect(pipe).toBeTruthy();
  });
});
