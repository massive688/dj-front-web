import { WorkScrollPositionDirective } from './work-scroll-position.directive';

describe('WorkScrollPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new WorkScrollPositionDirective();
    expect(directive).toBeTruthy();
  });
});
