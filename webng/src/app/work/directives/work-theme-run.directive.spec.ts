import { WorkThemeRunDirective } from './work-theme-run.directive';

describe('WorkThemeRunDirective', () => {
  it('should create an instance', () => {
    const directive = new WorkThemeRunDirective();
    expect(directive).toBeTruthy();
  });
});
