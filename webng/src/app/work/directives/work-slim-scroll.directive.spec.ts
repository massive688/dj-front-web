import { WorkSlimScrollDirective } from './work-slim-scroll.directive';

describe('WorkSlimScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new WorkSlimScrollDirective();
    expect(directive).toBeTruthy();
  });
});
